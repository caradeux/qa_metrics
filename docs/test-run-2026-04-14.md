# Test run — 2026-04-14

## Inventario de tests existentes

### Backend (API) — vitest · `apps/api/src/__tests__/`
- `auth.test.ts`
- `client-reports.test.ts`
- `daily-records.test.ts`
- `holidays.test.ts`
- `metrics.service.test.ts`
- `metrics.test.ts`
- `permissions.test.ts`
- `testers-me.test.ts`

Todos son de integración: usan `fetch` contra `http://localhost:4000` (requieren API levantada). **No son destructivos** — no hay `deleteMany` ni `truncate` en beforeAll.

### Frontend (web) — vitest · `apps/web/__tests__/`
Solo existe `lib/` (helpers). **Sin tests unitarios React/TSX.**

### E2E — Playwright · `apps/web/e2e/`
- `01-auth.spec.ts` · `02-clients.spec.ts` · `03-projects.spec.ts`
- `04-cycles-testers.spec.ts` · `05-records-manual.spec.ts` · `06-import.spec.ts`
- `07-dashboard.spec.ts` · `08-reports.spec.ts` · `09-users-roles-assignments.spec.ts`
- `10-layout-navigation.spec.ts` · `97-modal-check.spec.ts` · `98-debug-login.spec.ts`
- `99-smoke.spec.ts`
- **Nuevo**: `99-full-smoke.spec.ts` (creado en este run).

## Resultados

### API (vitest) — con API corriendo en `:4000`
```
Test Files: 7 failed | 1 passed (8)
Tests:      10 failed | 8 passed | 16 skipped (34)
Duration:   1.72s
```

- Única suite que pasa: `auth.test.ts` en sus asserts estructurales (el mismo test que intenta login con `admin@qametrics.com / QaMetrics2024!` falla con 401).
- Todas las demás fallan en `beforeAll` → `loginAs(admin@qametrics.com)` con **401 Credenciales inválidas**.

### Web (vitest)
```
Sin tests unitarios. Directorio __tests__ solo contiene helpers.
```

### E2E smoke nuevo (`99-full-smoke.spec.ts`)
```
1 passed · 3 failed
```
- ✅ `smoke: analista` — `b.benardis@inovabiz.com / Inovabiz.2026` funciona y navega `/mi-semana`, `/projects`, `/gantt` sin 500/404.
- ❌ `smoke: admin` — login 401.
- ❌ `smoke: client_pm` — login 401 (`demo@demo.cl`, password desconocido).
- ❌ `smoke: datos mínimos` — no puede validar porque requiere token admin.

> No se corrieron las suites E2E `01–10` completas: todas usan los mismos TEST_USERS desactualizados, el resultado sería análogo al smoke.

## Tests rotos por refactors / cambios recientes

**Causa raíz única**: las credenciales codificadas en los tests no coinciden con la BD actual.

| Test usa | Estado |
|---|---|
| `admin@qametrics.com / QaMetrics2024!` | ❌ 401 contra API actual |
| `laura.gomez@qametrics.com / Lead2024!` | usuario no existe en BD actual |
| `ana.garcia@qametrics.com / Analyst2024!` | usuario no existe en BD actual |
| `b.benardis@inovabiz.com / Inovabiz.2026` | ✅ funciona |

- Fuentes afectadas: `apps/web/e2e/helpers/auth.ts`, `apps/web/e2e/global-setup.ts`, `apps/api/src/__tests__/helpers/auth.ts`.
- **Sin refactor de UI detectado como causa** — los tests ni siquiera llegan a la navegación.

## Gaps de cobertura (features sin tests)

1. **Gantt** (`/gantt`) — renderer, drag, resize, reasignación. Sin tests.
2. **AssignmentPhases** (fases de asignación) — CRUD + logs de status. Sin tests unitarios ni E2E.
3. **DailyRecord** — endpoint tiene integration tests pero **no** cobertura de cálculos derivados (velocidad, acumulados).
4. **Reportes semanales** (`/reports/client`) — generación PDF/Excel: sin tests.
5. **Import Excel** — el E2E existe (`06-import.spec.ts`) pero depende de fixture no verificado.
6. **Roles/permissions matrix** — `permissions.test.ts` es muy estrecho, no valida cada combinación de rol × recurso.
7. **Middleware auth server-side** (Next.js middleware) — sin tests.
8. **CryptoService** (cifrado de PAT ADO) — sin tests unitarios.
9. **WeeklyRecord** (agrupación por semana, `startOfWeek`) — lógica crítica sin tests.
10. **Azure DevOps sync** (módulo M6) — sin tests con mocks de fetch.

## Top-10 tests prioritarios antes de producción

1. **Auth flow completo**: login válido/inválido, refresh token, logout, rutas protegidas.
2. **Role matrix**: por cada rol (ADMIN, LEADER, QA_ANALYST, CLIENT_PM) verificar 403/200 en cada endpoint sensible.
3. **Creación y ciclo de vida de proyecto**: cliente → proyecto → ciclo → HU → asignación.
4. **Asignación de tester**: validar phases, overallocation flag, statusLog generado.
5. **DailyRecord**: crear/editar/agregar validando `weekStart = lunes` y unique constraint.
6. **Metrics endpoint** (`/api/metrics`): KPIs con data conocida (fixtures), comparar valores exactos.
7. **Import Excel**: fila inválida detectada, duplicados rechazados, rollback en error.
8. **Generación de reportes**: PDF se genera, Excel tiene las 5 hojas esperadas con celdas pobladas.
9. **Gantt data endpoint**: rangos de fechas, solapamiento, feriados respetados.
10. **E2E full journey**: admin crea proyecto → asigna tester → tester carga daily → admin ve dashboard y exporta.

## Acción correctiva inmediata recomendada

- Centralizar los `TEST_USERS` en una variable de entorno (`.env.test`) o leer de `packages/database/prisma/seed.ts`.
- Añadir un job al seed que cree siempre los usuarios de test con passwords conocidos y active `role=ADMIN/LEADER/ANALYST/CLIENT_PM`.
- Marcar los tests de integración que dependen de API en un script `npm run test:integration` separado, y dejar `npm test` solo para unit tests con mocks.
